/**
 * A simple updater with one single PID.
 * Tries to adjust the device position from gyro and accel data.
 */

module.exports = function (quad) {
	var ctrl = quad.ctrl;

	return function (data) {
		// TODO
	};
};